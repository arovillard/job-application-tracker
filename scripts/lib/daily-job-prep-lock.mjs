import Database from "better-sqlite3";
import { randomUUID } from "node:crypto";
import { lstatSync } from "node:fs";
import path from "node:path";
import { assertCurrentOpportunitySchema } from "./current-opportunity-schema.mjs";
export const LOCK_TTL_MS = 6 * 60 * 60 * 1000;
export const LOCK_METADATA_KEY = "daily_job_prep_lock";
const UUID = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
function assertFile(p) { if (!path.isAbsolute(p)) throw new Error("database path must be absolute"); let stat; try { stat = lstatSync(p); } catch { throw new Error("database file must exist"); } if (!stat.isFile()) throw new Error("database path must be a regular file"); }
function parse(row) { if (!row) return null; let lock; try { lock = JSON.parse(row.value); } catch { throw new Error("malformed daily job prep lock"); } if (!lock || lock.schemaVersion !== 1 || !UUID.test(lock.token) || !Number.isFinite(lock.acquiredAt) || !Number.isFinite(lock.expiresAt) || lock.expiresAt <= lock.acquiredAt) throw new Error("malformed daily job prep lock"); return lock; }
function open(p, readonly = false) { assertFile(p); return new Database(p, { fileMustExist: true, ...(readonly ? { readonly: true } : {}) }); }
function read(db) { return parse(db.prepare("SELECT value FROM schema_metadata WHERE key = ?").get(LOCK_METADATA_KEY)); }
export function acquireDailyJobPrepLock(databasePath, now = Date.now()) { const db = open(databasePath); try { assertCurrentOpportunitySchema(db); db.exec("BEGIN IMMEDIATE"); try { const current = read(db); if (current && current.expiresAt > now) throw new Error("daily job prep lock is already held"); const lock = { schemaVersion: 1, token: randomUUID(), acquiredAt: now, expiresAt: now + LOCK_TTL_MS }; db.prepare("INSERT INTO schema_metadata(key, value) VALUES (?, ?) ON CONFLICT(key) DO UPDATE SET value=excluded.value").run(LOCK_METADATA_KEY, JSON.stringify(lock)); db.exec("COMMIT"); return { schemaVersion: 1, action: current ? "recovered" : "acquired", databasePath, ...lock }; } catch (error) { try { db.exec("ROLLBACK"); } catch {} throw error; } } finally { db.close(); } }
export function verifyDailyJobPrepLock(databasePathOrOpenDb, token, now = Date.now()) { const db = typeof databasePathOrOpenDb === "string" ? open(databasePathOrOpenDb, true) : databasePathOrOpenDb; const close = typeof databasePathOrOpenDb === "string"; try { assertCurrentOpportunitySchema(db); const lock = read(db); if (!lock || lock.token !== token || lock.expiresAt <= now) throw new Error("daily job prep lock is not held by this token"); return { schemaVersion: 1, action: "verified", ...lock }; } finally { if (close) db.close(); } }
export function releaseDailyJobPrepLock(databasePath, token) { const db = open(databasePath); try { assertCurrentOpportunitySchema(db); db.exec("BEGIN IMMEDIATE"); try { const lock = read(db); if (!lock || lock.token !== token) throw new Error("daily job prep lock is not held by this token"); db.prepare("DELETE FROM schema_metadata WHERE key = ?").run(LOCK_METADATA_KEY); db.exec("COMMIT"); return { schemaVersion: 1, action: "released", databasePath }; } catch (error) { try { db.exec("ROLLBACK"); } catch {} throw error; } } finally { db.close(); } }
