"use client";

import { useState } from "react";

import { CONNECTION_STATUSES, OPPORTUNITY_PRIORITIES, RELATIONSHIP_STRENGTHS, type ConnectionOpportunity, type ConnectionOpportunityInput, type OpportunityActivityInput, type OpportunityTaskInput } from "../types";
import { CONNECTION_STATUS_LABELS } from "./OpportunityTable";

export type ConnectionFormState = { label: string; organization: string; roleContext: string; contactInfo: string; meetingContext: string; summary: string; relationshipStrength: ConnectionOpportunityInput["relationshipStrength"]; status: ConnectionOpportunityInput["status"]; priority: NonNullable<ConnectionOpportunityInput["priority"]>; activityType: OpportunityActivityInput["type"]; activityBody: string; activityDate: string; taskTitle: string; taskDueDate: string; };
export type ConnectionCreationPayload = { opportunity: ConnectionOpportunityInput; initialActivity: OpportunityActivityInput | null; initialTask: OpportunityTaskInput | null };
export type ConnectionOpportunityFormMode = "create" | "edit";

export function buildConnectionCreationPayload(state: ConnectionFormState): ConnectionCreationPayload { return { opportunity: { type: "connection", label: state.label.trim(), organization: state.organization.trim() || null, roleContext: state.roleContext.trim() || null, contactInfo: state.contactInfo.trim() || null, meetingContext: state.meetingContext.trim() || null, summary: state.summary.trim() || null, relationshipStrength: state.relationshipStrength, status: state.status, priority: state.priority }, initialActivity: state.activityBody.trim() ? { type: state.activityType, body: state.activityBody.trim(), occurredAt: state.activityDate ? `${state.activityDate}T12:00:00.000Z` : new Date().toISOString() } : null, initialTask: state.taskTitle.trim() ? { title: state.taskTitle.trim(), dueDate: state.taskDueDate || null } : null }; }
const empty: ConnectionFormState = { label: "", organization: "", roleContext: "", contactInfo: "", meetingContext: "", summary: "", relationshipStrength: "new", status: "new", priority: "medium", activityType: "note", activityBody: "", activityDate: "", taskTitle: "", taskDueDate: "" };

export function ConnectionOpportunityForm({ onSubmit, isSubmitting = false, initialValue, error, submitLabel = "Create connection", mode, onCancel }: { onSubmit: (payload: ConnectionCreationPayload) => void | Promise<void>; isSubmitting?: boolean; initialValue?: ConnectionOpportunity | ConnectionOpportunityInput | null; error?: string | null; submitLabel?: string; mode?: ConnectionOpportunityFormMode; onCancel?: () => void; }) {
  const resolvedMode = mode ?? (initialValue ? "edit" : "create");
  const [state, setState] = useState<ConnectionFormState>(() => ({ ...empty, ...initialValue, organization: initialValue?.organization ?? "", roleContext: initialValue?.roleContext ?? "", contactInfo: initialValue?.contactInfo ?? "", meetingContext: initialValue?.meetingContext ?? "", summary: initialValue?.summary ?? "" }));
  const [detailsOpen, setDetailsOpen] = useState(resolvedMode !== "create" && Boolean(initialValue?.organization || initialValue?.roleContext || initialValue?.contactInfo || initialValue?.meetingContext || initialValue?.summary));
  const set = <K extends keyof ConnectionFormState>(key: K, value: ConnectionFormState[K]) => setState((current) => ({ ...current, [key]: value }));
  const input = (key: keyof ConnectionFormState, label: string, required = false, type: "text" | "date" = "text", autoComplete?: string) => {
    const helperId = key === "label" ? "connection-label-helper" : undefined;
    return <label className="application-form__field"><span className="application-form__label">{label}{required ? <span> Required</span> : null}</span><input autoComplete={autoComplete} className="application-form__input" required={required} type={type} aria-describedby={helperId} value={state[key] as string} onChange={(event) => set(key, event.target.value as never)} />{helperId ? <span id={helperId} className="application-form__helper">A name is required to identify this connection.</span> : null}</label>;
  };
  const payload = () => { const created = buildConnectionCreationPayload(state); return resolvedMode === "create" ? created : { ...created, initialActivity: null, initialTask: null }; };
  return <form className="application-form" onSubmit={(event) => { event.preventDefault(); void onSubmit(payload()); }}><div className="application-form__body">{error ? <p role="alert">{error}</p> : null}<div className="application-form__intro"><strong>Connection opportunity</strong><span>Capture the relationship and plan your next move.</span></div><div className="application-form__grid">{input("label", "Person's name", true, "text", "name")}</div><fieldset className="application-form__fieldset application-form__planning"><legend>Plan your Next move</legend><div className="application-form__grid"><label className="application-form__field"><span className="application-form__label">Stage</span><select className="application-form__select" value={state.status} onChange={(event) => set("status", event.target.value as ConnectionFormState["status"])}>{CONNECTION_STATUSES.map((status) => <option key={status} value={status}>{CONNECTION_STATUS_LABELS[status]}</option>)}</select></label><label className="application-form__field"><span className="application-form__label">Priority</span><select className="application-form__select" value={state.priority} onChange={(event) => set("priority", event.target.value as ConnectionFormState["priority"])}>{OPPORTUNITY_PRIORITIES.map((priority) => <option key={priority}>{priority}</option>)}</select></label>{resolvedMode === "create" ? <><label className="application-form__field"><span className="application-form__label">First task</span><input className="application-form__input" value={state.taskTitle} onChange={(event) => set("taskTitle", event.target.value)} /></label>{input("taskDueDate", "Due date", false, "date")}</> : null}</div></fieldset><details className="form-disclosure" open={detailsOpen} onToggle={(event) => setDetailsOpen(event.currentTarget.open)}><summary>Optional details <span>Optional</span></summary><div className="application-form__grid form-disclosure__grid">{input("organization", "Organization", false, "text", "organization")}{input("roleContext", "Role or context")}{input("contactInfo", "Contact information", false, "text", "email")}{input("meetingContext", "Where or how you met")}<label className="application-form__field"><span className="application-form__label">Relationship strength</span><select className="application-form__select" value={state.relationshipStrength} onChange={(event) => set("relationshipStrength", event.target.value as ConnectionFormState["relationshipStrength"])}>{RELATIONSHIP_STRENGTHS.map((strength) => <option key={strength}>{strength}</option>)}</select></label></div><label className="application-form__field"><span className="application-form__label">Summary</span><textarea className="application-form__textarea" value={state.summary} onChange={(event) => set("summary", event.target.value)} /></label>{resolvedMode === "create" ? <fieldset className="application-form__fieldset"><legend>Initial interaction</legend><div className="application-form__grid"><label className="application-form__field"><span className="application-form__label">Type</span><select className="application-form__select" value={state.activityType} onChange={(event) => set("activityType", event.target.value as ConnectionFormState["activityType"])}>{["note", "meeting", "call", "email", "message", "introduction"].map((type) => <option key={type}>{type}</option>)}</select></label>{input("activityDate", "Date", false, "date")}{input("activityBody", "What happened?")}</div></fieldset> : null}</details></div><div className="application-form__actions">{onCancel ? <button className="button" type="button" onClick={onCancel}>Cancel</button> : null}<button className="button button--primary" disabled={isSubmitting} type="submit">{isSubmitting ? "Saving…" : submitLabel}</button></div></form>;
}
